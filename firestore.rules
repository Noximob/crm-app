rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras completamente abertas para desenvolvimento e teste
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 