'use client';

/** FUNIL DO IMÓVEL — a página. O trabalho todo mora em ../funis.tsx. */
import SetorLocacao from '../funis';

export default function PaginaImoveis() {
  return <SetorLocacao funil="imoveis" />;
}
