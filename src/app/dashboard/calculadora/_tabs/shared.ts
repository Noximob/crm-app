/** Dados do imóvel digitados UMA vez no topo da Calculadora e usados pelas duas abas. */
export type HeaderImovel = {
  empreendimento: string;
  unidade: string;
  torre: string;
  cliente: string;
  valorImovel: number;
};
